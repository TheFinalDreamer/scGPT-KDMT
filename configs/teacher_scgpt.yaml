# Teacher fine-tuning configuration

data_path: "outputs/processed/pbmc10x"
output_dir: "outputs/teacher"
seed: 42

# Model architecture (large: 12 layers, 512-dim, ~51.3M params)
input_dim: 16545
teacher_hidden_dim: 512
teacher_num_heads: 8
teacher_num_layers: 12
num_classes: 31

# Training
batch_size: 64
num_epochs: 10
lr: 0.0001
weight_decay: 0.0001